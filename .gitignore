# Python
.venv/
venv/
__pycache__/
*.py[cod]
*$py.class
.Python
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.egg-info/
dist/
build/
outputs/    

# OS / editor
.DS_Store
.idea/
.vscode/

# Local runtime / secrets (do not commit)
.env
.env.*
!.env.example
*.log

# Regenerable caches and large training artifacts (repo stays source-focused)
.cache/
data_cache.npz
Runs/
config/cohorts/
paper/
# Broker credentials and local execution state (reserved for the tier-5 shadow
# ledger and any future broker integration; never tracked)
execution/**

# Frontend: source is tracked; only installs and build output are ignored
frontend/node_modules/
frontend/dist/
frontend/coverage/
# Regenerated by scripts/publish_frontend_data.py (and frontend/scripts/dev.mjs)
frontend/public/data/